type BookType = {
  id: string;
  title: string;
  content: string;
  price: number;
  thumbnail: { url: string };
  createdAt: string;
  publishedAt: string;
  updatedAt: string;
};

export type { BookType };
